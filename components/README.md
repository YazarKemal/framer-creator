# Components

Framer Code Components — reusable React + TypeScript modules that ship inside
Framer projects and are customisable through Property Controls.

## Placeholder

Components will be added here as they are developed. Each component lives in
its own file (`ComponentName.tsx`) and follows the authoring pattern documented
in the root `CLAUDE.md`.

### Component index

| Component      | Description                     | Status    |
|----------------|---------------------------------|-----------|
| —              | No components created yet.      | —         |

### Adding a component

1. Create `components/<ComponentName>.tsx` using the skeleton from `CLAUDE.md`.
2. Add a row to the table above.
3. Test at desktop, tablet, and mobile breakpoints.
4. Run through the `docs/marketplace-checklist.md` before shipping.
